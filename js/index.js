$(document).ready(function(){
$('.owl-carousel').owlCarousel({
    loop:true,
    margin:100,
    autoplay:true,
    autoplayHoverPause:true,
    autoplayTimeout:5000,
    animateIn: true,
    items:4
})
});